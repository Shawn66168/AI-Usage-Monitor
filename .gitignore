.DS_Store
.build/
DerivedData/
*.xcuserstate
*.xcuserdata/
Resources/dashboard-preview.png

# Keep packaged local release artifacts in Build for delivery.
# Remove the following exception if releases are distributed elsewhere.
!Build/
!Build/**
